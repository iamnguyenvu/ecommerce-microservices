spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driverClassName: org.h2.Driver
    username: sa
    password: 
  
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop
    show-sql: false  # Giảm log noise trong test
  
  h2:
    console:
      enabled: true

jwt:
  secret: test-secret-key-for-testing-should-be-at-least-256-bits-long
  expiration: 86400000
